<Window x:Class="FractionTrainer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FractionTrainer"
        mc:Ignorable="d"
        Title="Тренажёр собрать дробь" 
        Height="720" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="#FFEAEAEA">
    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF007AFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF0056B3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF003D80"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTitleText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="#FF4A4A4A"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,25"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   FontSize="32" FontWeight="Bold" Foreground="#FF007AFF" 
                   HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"
                   Margin="0,35,0,25">
            Тренажёр<LineBreak/>Собери дробь
        </TextBlock>

        <StackPanel Grid.Row="1" 
                    HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">

            <TextBlock Text="Выберите режим:" Style="{StaticResource ModernTitleText}"/>

            <Button x:Name="LearningModeButton" Content="Обучение" 
                    Width="220" Height="50" 
                    Style="{StaticResource ModernButton}" 
                    Click="LearningModeButton_Click"/>

            <Button x:Name="MultipleChoiceModeButton" Content="Выберите варианты" 
                    Width="220" Height="50" 
                    Style="{StaticResource ModernButton}" 
                    Click="MultipleChoiceModeButton_Click"/>

            <Button x:Name="FindPairsModeButton" Content="Найдите пары" 
                    Width="220" Height="50" 
                    Style="{StaticResource ModernButton}" 
                    Click="FindPairsModeButton_Click"/>

            <Button x:Name="TestModeButton" Content="Проверка знаний" 
                    Width="220" Height="50" 
                    Style="{StaticResource ModernButton}" 
                    Click="TestModeButton_Click"/>
        </StackPanel>
    </Grid>
</Window>